# ---- 权重与训练产物（体积大，不进 git）----
# 权重发布走 GitHub Release（单文件超 100MB，普通 git 推不上去），见 README「模型权重下载」。
# weights/ 是本地缓存目录：下载回来的归档放这里，只做本地使用，不上传。
checkpoints/
outputs/
weights/*.tar.gz
*.safetensors
*.tar.gz
*.pt
*.pth

# ---- 数据集（走 HuggingFace，见 README）----
data/lerobot_*/
data/octo_frames/
data/octo_tfds*/
*.parquet
*.mp4
*.avi
*.mov

# 例外：演示素材是要提交的
# 用 GIF 而不是 mp4/avi —— GitHub 的 README 渲染器会过滤掉 <video> 标签，
# 仓库内的视频文件无法直接内嵌播放，GIF 是唯一能「点开就播」的形式。
!assets/videos/*.gif
!assets/videos/*.mp4

# ---- 标注工程中间产物 ----
annotate/
yolo_dataset/
yolo_runs/

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv*/
*.egg-info/
.ipynb_checkpoints/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
.DS_Store
Thumbs.db
~$*.docx

# ---- 临时调试 ----
_tmp_*
_tmp*/
*.log
!assets/**/*.log
